# Run instructions
1. Install dependencies
`cd frontend`
`npm install`

2. Run frontend and backend
`cd frontend`
`npm run dev`
`cd ../chatbot`
`python server.py`

3. Run chatbot using CLI
'cd medical-chatbot'
'python chatbot.py'